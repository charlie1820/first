<%= form_with(model: charla, local: true) do |form| %>
  <% if charla.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(charla.errors.count, "error") %> prohibited this charla from being saved:</h2>

      <ul>
      <% charla.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
